# Bounded external-blocker matrix

| Candidate | Repository evidence | Expected Hostinger symptom | Current observation | Status | Exact maintainer evidence needed | Smallest correction if confirmed |
|---|---|---|---|---|---|---|
| Wrong start command | `package.json` start is `next start`; no root `server.js` exists. | Static/cached shell may serve; APIs 503/timeout; no `[startup]` lines. | Recovered logs show Next.js startup, bind `0.0.0.0:3000`, startup lines, and Ready. | Eliminated for recovered deployment | None unless Hostinger start command changes. | None. |
| Migrations never invoked | Migrations are invoked only through `src/instrumentation.ts`; not by the npm script itself. | Server might start against stale schema, or runtime lacks startup migration logs. | Recovered logs show `[startup] Applying database migrations…` and `[startup] Migrations applied.`; both migrations recorded. | Eliminated for recovered deployment | None. | None. |
| Migration failure before server start | `runStartup` returns non-zero and instrumentation calls `process.exit` when migration fails. | 503/timeout before health; logs should show migration failure if attached. | Recovered logs show migrations applied and Ready; `canonical_json` is `MEDIUMTEXT NOT NULL`. | Eliminated for recovered deployment | None. | None. |
| Migration files absent from deployed artifact | `next.config.mjs` includes `./src/db/migrations/**`; resolver fails closed if `_journal.json` absent. | 503 before health; log mentions missing `meta/_journal.json` or attempted paths. | Recovered deployment applied both committed migrations. | Eliminated for recovered deployment | None. | None. |
| Wrong working directory | Resolver handles repo root, `.next/standalone`, relocated standalone root, and a few parents. | Missing migration journal despite files existing elsewhere. | No logs to confirm. | Externally unverified, less likely from source | Runtime cwd, start command, and migration error attempted paths. | Start from repo root for `next start`, or from standalone root for `node server.js`; otherwise set `LABEL_LENS_MIGRATIONS_DIR`. |
| Missing `DATABASE_URL` | `src/db/client.ts` throws immediately when absent; instrumentation imports DB client before `runStartup` can log structured phase. | Crash before health, possibly before `[startup]` logs; 503/timeout. | Recovered deployment applied migrations and serves DB-backed auth. | Eliminated for recovered deployment | None. | None. |
| DB connection failure | `applyMigrations` opens MySQL connection through `mysql2/promise`. | Startup migration failure; no health. | Recovered deployment applied migrations, login works, and package finalization persisted. | Eliminated for recovered deployment | None. | None. |
| Missing required secret | `LABEL_LENS_APPEND_SIGNING_KEY` is route-use for precheck/analyze; `LABEL_LENS_INTEGRITY_SECRET` is route-use for finalization/detail; `BETTER_AUTH_SECRET` is required by docs and imported with auth; `LABEL_LENS_STORAGE_DIR` is route-use for production finalization. | Missing append key should still allow `/api/health` 200 with `appendSigningKeyConfigured:false`; missing DB/auth secret may affect startup/auth; missing storage affects finalization. | Health does not execute, so append/storage alone cannot explain current 503. | Mixed: append/storage eliminated as sole health blocker; auth secret externally unverified | Presence/minimum-length checks only. Do not reveal values. | Set missing/short secret in Hostinger; redeploy. Do not rotate existing signing secrets unless explicitly approved. |
| Bootstrap failure | Bootstrap runs only when `LABEL_LENS_BOOTSTRAP_ON_START=1`; missing/invalid bootstrap values fail closed. | Logs show bootstrap phase failure or process unavailable before health. | Controlled config change from `1` to `0` recovered runtime with all other observed conditions unchanged. Existing admin/agent/seller accounts confirmed. | Confirmed boundary at configuration level; exact exception unknown | None for recovery. Preserve failed-run uncertainty. | Leave `LABEL_LENS_BOOTSTRAP_ON_START=0` because accounts already exist. Do not reset passwords. |
| Incorrect `PORT` / `HOSTNAME` binding | No custom server; Next/standalone owns binding. Docker sets `HOSTNAME=0.0.0.0`; docs say server reads `PORT`. | Process may be running but proxy returns 503/timeout. | Recovered logs show bind `0.0.0.0:3000`; health/login work. | Eliminated for recovered deployment | None. | None. |
| Server crash before logging | Import-time `DATABASE_URL` failure or native/package failure can occur before structured startup logs. | Runtime logs may be empty if logger is unattached; dynamic routes 503. | Runtime logs page says no logs found. | Plausible / externally unverified | Process exit/restart history, build/runtime logs, stderr capture source. | Correct the first logged import/runtime failure; if logs unattached, fix Hostinger log attachment or use build/deployment logs. |
| Hostinger proxy/upstream mismatch | App source has no proxy config; Hostinger controls upstream routing. | Dashboard says Running; root may be cached; dynamic routes 503/timeout. | Recovered `/api/health`, `/login`, auth, finalize, agent detail, and panel delivery work. | Eliminated for recovered deployment | None. | None. |
| Cached static shell masking dead Node runtime | Static root 200 while dynamic routes fail is explicitly possible. | `/` 200; `/api/health` 503; guarded routes timeout. | Confirmed symptom pattern. | Plausible supporting condition, not root cause by itself | CDN/cache headers and process health at same timestamp. | Do not use `/` as health proof; use `/api/health` and auth endpoint. |
| Hostinger runtime logging not attached to actual process | Repo emits non-secret startup logs. | No runtime logs even when process fails or never starts. | Logs are visible after the recovered deploy, but failed-run logs were not preserved. | Partially eliminated going forward; failed-run internals remain unknown | Preserve recovered logs; do not infer failed-run exception. | None for Issue #162. |
| Stale deployed branch/commit | Hostinger shows branch `main`, commit `c3dfb942`. | Would run pre-#161 code. | Dashboard confirms `c3dfb942`. | Eliminated at dashboard level | None unless dashboard evidence changes. | None. |
| Build failure | Hostinger latest deployment state is Completed. | No current deployed artifact or old artifact would remain. | Dashboard says Completed. | Eliminated at dashboard level; build logs still useful | Latest build/deployment logs for command details and warnings. | None unless build logs reveal partial/fallback behavior. |
| Unsupported Node major | `package.json` engines and `.nvmrc` require Node 22; Hostinger shows Node 22.x. | Install/build/runtime failure. | Dashboard Node 22.x. | Eliminated at dashboard level | None unless runtime uses different Node than dashboard. | None. |

## Current leading blockers

The first failing boundary is now attributed to bootstrap-enabled startup. The exact internal bootstrap exception remains unresolved because failed-run logs were not preserved. No repository-only correction is justified inside Issue #162; leave bootstrap disabled because accounts already exist.
