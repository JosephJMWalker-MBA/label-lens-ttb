# Hypothesis Matrix

| Hypothesis | Predicted result | Observed result | Can explain deployed observation? | Evidence | Remaining uncertainty |
| --- | --- | --- | --- | --- | --- |
| Stable configured secret across module restart | Valid record verifies and opens after module recreation. | Passed on MySQL and SQLite. Agent detail opened and authorized panel bytes streamed. | No, if production secret was stable and bytes were not altered. | `src/app/api/package/submit/finalize/route.test.ts:526-575`; `npm run test:mysql`; SQLite finalize run. | Does not prove deployed key history. |
| Changed configured secret across restart | Existing record fails verification. | Unit test observed fail-closed verification after changing the configured secret. | Yes, if production secret changed after write. | `src/lib/integrity.test.ts:67-76`. | No production secret history was available. |
| Missing or short production secret | Signing/verifying throws instead of silently accepting. | Unit test observed production throw for missing and short secrets. | Could make existing records unreadable or block writes if production config was lost. | `src/lib/integrity.ts:25-29`; `src/lib/integrity.test.ts:78-90`. | No deployed env snapshot was available. |
| Development ephemeral secret across process restart | Non-production no-secret signatures differ across processes. | Child-process test observed different non-reversible signature digests. | Yes for seed/dev-created records later read by another process. Not a valid production explanation if production env was configured. | `src/lib/integrity.ts:31-39`; `src/lib/integrity.test.ts:104-111`. | No evidence the deployed failing record came from this path. |
| E2E seed/demo writer without configured secret | Previously could write a record that another process could not verify. | Guard now rejects before DB side effects unless explicit configured secret exists. | Possible only if the deployed record came from a no-secret seed/dev process. | `scripts/e2e-seed.ts:15-17`; `src/lib/integrity.test.ts:92-130`. | Deployed record provenance was unavailable. |
| Canonical bytes mutate between sign and insert in route | Read-back bytes differ from pre-insert bytes. | Lifecycle test observed stored canonical bytes and signature unchanged after module recreation. | No for the tested production route. | `src/app/api/package/submit/finalize/route.test.ts:526-575`. | Does not cover arbitrary historical rows. |
| Signature storage truncates or re-encodes | Stored signature is malformed or too short. | Signature format is 67 characters with `v1` version; MySQL `varchar(255)` is sufficient. | No for this mechanism alone. | `src/db/schema.ts:104-105`; `src/lib/integrity.test.ts:53-65`. | Historical row value unknown. |
| MySQL `TEXT` strict mode | Insert of a valid canonical payload over 65,535 bytes fails before commit. | Pre-fix strict-mode reproduction returned finalize `500` with bounded `ER_DATA_TOO_LONG` cause chain. | Not the exact deployed "waiting row opens then integrity_failed" shape, because no waiting row should commit. | Local strict MySQL focused reproduction; exact SQL mode in `reproduction.md`. | Exact deployed SQL mode unknown. |
| MySQL `TEXT` permissive mode | Insert commits but truncates canonical bytes to 65,535, so read verification fails. | Pre-fix permissive reproduction committed a row with `canonicalBytes=65535`, signature length 67, version `v1`, and `verifies=false`. | The permissive result can explain the deployed symptom shape if deployed MySQL was permissive and payload exceeded `TEXT`. | Local permissive MySQL bounded verifier check; exact SQL mode in `reproduction.md`. | Deployed SQL mode and row byte length were unavailable. |
| SQLite parity | SQLite should preserve the synthetic canonical string. | SQLite finalize plus integrity tests passed. | No, if deployment used MySQL as documented. | SQLite test run; `src/db/schema.sqlite.ts:99-112`. | SQLite was not the deployed authoritative dialect. |
| Migration/bootstrap behavior | Startup should apply committed MySQL migrations idempotently. | MySQL startup integration passed after adding `0001`. | Reduces future risk after deployment. | `src/server/startup.test.ts:174-238`; `npm run test:mysql`. | Does not prove current production has already applied `0001`. |
| Existing valid row upgraded from `TEXT` to `MEDIUMTEXT` | Existing canonical bytes and signature should remain unchanged and still verify. | MySQL upgrade-path test inserted a valid signed row under `0000`, opened it, applied `0001`, confirmed exact canonical bytes/signature unchanged, and opened it again. | Proves the migration is non-destructive for valid below-limit rows. | `src/server/migrate.test.ts`; focused MySQL upgrade test. | Does not repair rows that were already invalid before migration. |
| Deliberately corrupted stored bytes | Detail should fail closed, with no bypass or silent resign. | Existing route tests continue to cover corrupted canonical rows as fail-closed. | Yes as a generic class, but no evidence of independent corruption. | `src/server/submissions/detail.ts:130-133`; finalize and agent portal test suites. | Historical row was not inspected. |
| Deliberately truncated stored bytes after migration | Detail should continue returning `integrity_failed`. | MySQL upgrade-path test deliberately truncated the post-migration row and observed `integrity_failed`. | Preserves the historical-corruption boundary. | `src/server/migrate.test.ts`. | The deployed failing row was not modified or adjudicated. |
