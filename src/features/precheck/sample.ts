/**
 * Suggested declared values for the bundled M Cellars demonstration fixture.
 * These are declared application values a reviewer can edit — never a bypass of
 * extraction, and never the fixture's truth labels.
 */
export const SAMPLE_DECLARED = {
  brand: "M CELLARS",
  alcohol: "12.5",
} as const;
