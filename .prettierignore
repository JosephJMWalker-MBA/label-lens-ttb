node_modules/
.next/
out/
coverage/
playwright-report/
test-results/
package-lock.json

# Markdown docs are authored by hand and versioned separately from code style.
*.md

# Binary fixture artifacts have no source formatting.
tests/fixtures/**/*.png
tests/fixtures/**/*.jpeg
tests/fixtures/**/*.jpg

# Vendored OCR language data (binary).
src/pipeline/extractor/assets/*.traineddata

# Generated evaluation baseline report (produced by `npm run eval:baseline`).
docs/extraction-baseline/

# Generated Drizzle migration artifacts (authored by drizzle-kit in its own style).
src/db/migrations/

# Vendored upstream model configuration, committed verbatim from a pinned
# Hugging Face revision for the Issue #149 PP-OCRv6 probe. Its bytes are hashed
# and gated, so reformatting it would break its recorded identity.
artifacts/issue-149-ppocrv6-small-onnx-compatibility-probe/vendor/inference.yml
