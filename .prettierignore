node_modules/
.next/
out/
coverage/
playwright-report/
test-results/
package-lock.json

# Markdown docs are authored by hand and versioned separately from code style.
*.md

# Binary fixture artifacts have no source formatting.
tests/fixtures/**/*.png
tests/fixtures/**/*.jpeg
tests/fixtures/**/*.jpg

# Vendored OCR language data (binary).
src/pipeline/extractor/assets/*.traineddata

# Generated evaluation baseline report (produced by `npm run eval:baseline`).
docs/extraction-baseline/

# Generated Drizzle migration artifacts (authored by drizzle-kit in its own style).
src/db/migrations/

# Vendored upstream model configuration, committed verbatim from a pinned
# Hugging Face revision for the Issue #149 PP-OCRv6 probe. Its bytes are hashed
# and gated, so reformatting it would break its recorded identity.
artifacts/issue-149-ppocrv6-small-onnx-compatibility-probe/vendor/inference.yml
artifacts/issue-149-brand-ppocrv6-small-onnx-contrast/vendor/inference.yml

# Frozen raw inference evidence. Every file's SHA-256 is recorded in
# raw-output-manifest.json before the truth boundary is crossed, so reformatting
# would invalidate the manifest that proves the evidence is unaltered.
artifacts/issue-149-brand-ppocrv6-small-onnx-contrast/raw/
artifacts/issue-149-brand-complete-evidence-acquisition/runtime/truth-key-inventory.json

# Issue #149 Stage 1 generator output. These three files are compared
# BYTE-FOR-BYTE against a regeneration by
# scripts/eval/issue-149-brand-evidence-acquisition-freeze.mjs --check, so
# reformatting them would break the reproducibility gate.
artifacts/issue-149-brand-complete-evidence-acquisition/truth-free-input-manifest.json
artifacts/issue-149-brand-complete-evidence-acquisition/population-freeze.json
artifacts/issue-149-brand-complete-evidence-acquisition/post-freeze/id-map.json

# Immutable Issue #149 scientific evidence, committed byte-for-byte from the
# verified workflow artifacts for run 30775581351. These files include JSONL
# streams with historical .json suffixes and must never be parsed, formatted,
# rewritten, or normalized by Prettier.
artifacts/issue-149-brand-complete-evidence-result-30775581351/
